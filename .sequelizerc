// .sequelizerc
const path = require("path");

module.exports = {
  config: path.resolve("./src/app/share/configs/db.config.js"), // File cấu hình kết nối database
  "models-path": path.resolve("./src/app/v1/models"), // Thư mục chứa models
  "migrations-path": path.resolve("./migrations"), // Thư mục chứa migrations
  "seeders-path": path.resolve("./seeders"), // Thư mục chứa seeders
};
